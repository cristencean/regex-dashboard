'use client';

import { AppState } from '@/store';
import { useSelector, useDispatch } from 'react-redux';

const ApprovalRegex: React.FC = () => {

    return (
        <div>

        </div>
    );
};

export default ApprovalRegex;